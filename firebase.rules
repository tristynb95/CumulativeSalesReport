rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to the dailySales collection
    match /dailySales/{day} {
      allow read: if true;
      allow write: if false; // Deny any client-side writes
    }
  }
}